# Note: This should be encrypted with sops after creation
secrets:
    secret:
        enabled: true
        stringData:
            # Add any sensitive environment variables here
            # MAINTAINERR__KEY: value
